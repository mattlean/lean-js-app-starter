/**
 * This config file only affects Jest tests.
 * To configure how Babel handles the application build,
 * configure the webpack config's babel-loader instead.
 */
module.exports = {
  presets: ['@babel/preset-env'],
}
