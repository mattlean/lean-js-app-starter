version: '3.8'
services:
    app:
        build: .
        image: 'ljas-basic-nodejs'
        container_name: 'ljas-basic-nodejs'
        restart: always
        ports:
            - '${PORT}:${PORT}'
        volumes:
            - .:/code
            - /code/node_modules
