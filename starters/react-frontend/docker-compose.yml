version: '3.8'
services:
    app:
        build: .
        image: 'ljas-react-frontend'
        container_name: 'ljas-react-frontend'
        restart: always
        ports:
            - '${PORT}:${PORT}'
        volumes:
            - .:/code
            - /code/node_modules
