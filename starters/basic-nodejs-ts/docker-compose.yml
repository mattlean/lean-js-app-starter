version: '3.8'
services:
    app:
        build: .
        image: 'ljas-basic-nodejs-ts'
        container_name: 'ljas-basic-nodejs-ts'
        restart: always
        ports:
            - '${PORT}:${PORT}'
        volumes:
            - .:/code
            - /code/node_modules
