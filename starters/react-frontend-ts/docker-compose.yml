version: '3.8'
services:
    app:
        build: .
        image: 'ljas-react-frontend-ts'
        container_name: 'ljas-react-frontend-ts'
        restart: always
        ports:
            - '${PORT}:${PORT}'
        volumes:
            - .:/code
            - /code/node_modules
